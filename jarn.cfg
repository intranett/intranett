[buildout]
extends =
    production.cfg
    cfgs/ejabberd.cfg
    cfgs/jarn.cfg

parts +=
    ${buildout:ejabberd-parts}

develop =
    src/intranett.policy
    src/intranett.theme
    src/intranett.tour
    src/jarn.extranet
    src/Products.Extropy
    src/Products.Invoice

[ejabberd]
erlang-path = /usr/bin

[ejabberd-conf]
xmppdomain = localhost

[supervisor-conf]
input = ${buildout:directory}/templates/supervisord-jarn.conf

[instance-base]
eggs +=
    psycopg2
    pylibmc
rel-storage =
    type postgresql
    dsn dbname='db1' user='zodbuser' host='jarn12.srv.rzob.gocept.net' password='KCn25qUDfRoO}aVn'
    shared-blob-dir false
    blob-dir ${buildout:directory}/var/blobcache
    blob-cache-size 512mb
    poll-interval 60
    cache-local-mb 0
    cache-prefix db1
    cache-servers 127.0.0.1:11211

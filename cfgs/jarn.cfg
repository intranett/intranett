[buildout]

[instance-base]
eggs =
    intranett.policy
    Products.Extropy
    collective.indexing

[test]
eggs +=
    jarn.extranet
    Products.Extropy
    Products.Invoice
defaults = ['--auto-color', '--auto-progress', '-q']
